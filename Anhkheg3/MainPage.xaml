<Page
    x:Class="Anhkheg3.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Anhkheg3"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <CommandBar>
                <AppBarButton Icon="Add" Label="Add Vehicle"/>
            </CommandBar>
            <ListView Name="Vehicles">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Image Source="Assets/StoreLogo.png" Width="50" Height="50"/>
                            <StackPanel Orientation="Vertical" Height="50">
                                <TextBlock Text="This is the name of the vehicle"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       Foreground="Green"
                                       IsTextSelectionEnabled="True"/>
                                <Border Background="Red">
                                    <TextBlock Text="This is the year, make, and model of the vehicle"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       IsTextSelectionEnabled="True"/>
                                </Border>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>
